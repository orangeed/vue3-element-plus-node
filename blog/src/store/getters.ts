const getters = {
    isPhone: (state: any) => state.app.isPhone,
    showMenu: (state: any) => state.app.showMenu,
    dialogVisible: (state: any) => state.app.dialogVisible,
    titleInfo: (state: any) => state.app.titleInfo
}

export default getters