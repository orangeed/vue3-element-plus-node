// 数组
const arr: (string | number)[] = [1, 'a']

// 元组
const arr1: [number, string] = [1, 'a']

// 元组在csv文件数据类型使用，元组整体而言用的很少了
const arr2: [string, string, number][] = [
    ['orange', 'aaa', 18],
    ['alex', 'bbb', 20]
]
export { }